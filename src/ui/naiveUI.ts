import {
    // create naive ui
    create,
    // component
    NButton,
    NForm,
    NFormItem,
    NInput,
    NIcon,
    NTooltip,
    NPopover
} from 'naive-ui'

const naive = create({
    components: [
        NButton,
        NForm,
        NFormItem,
        NInput,
        NIcon,
        NTooltip,
        NPopover
    ]
})

export default naive